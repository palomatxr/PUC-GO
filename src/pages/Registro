import Image from 'next/image'
import Head from 'next/head'
import { useEffect } from 'react';

export default function Home() {
  // Função para redirecionar para a página de login
  const redirectToLogin = () => {
    window.location.href = '/src/pages/login'; // Substitua pelo caminho real da sua página de login
  }
  const [nome,setNome] = useState('');
  const [email,setEmail] = useState('');
  const [senha,setSenha] = useState('');
  const [genero,setGenero] = useState('');
  const [tel,setTel] = useState('');
  
  useEffect(() => {
    // Adiciona os manipuladores de eventos aos botões no momento da montagem do componente
    const goButton = document.getElementById("login");

     const nomeChange = (e) => {
      setNome(e.target.value);
    };
    const emailChange = (e) => {
      setEmail(e.target.value);
    };
    const senhaChange = (e) => {
      setSenha(e.target.value);
    };
    const generoChange = (e) => {
      setGenero(e.target.value);
    };
    const telChange = (e) => {
      setTel(e.target.value);
    };

    
    
    if (loginButton) {
      gologinButton.addEventListener("click", redirectToLogin);
    }
    
  }, []);

    return (
    <div>
      <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>replit</title>
        <link href="style.css" rel="stylesheet" type="text/css" />
      </head>

    <div class="Registro">
      <label for="html">Nome</label><br>
      <input type="text" name="Nome" size="40" value={nome} onChange={nomeChange}/> 
      <label for="html">Email</label><br>
      <input type="text" name="Email" size="40" value={email} onChange={emailChange}/> 
      <label for="html">Senha</label><br>
      <input type="text" name="Senha" size="40" value={senha} onChange={senhaChange}/> 
      <label for="html">Gênero</label><br>
      <input type="text" name="Gênero" size="40" value={genero} onChange={generoChange}/> 
      <label for="html">Telefone</label><br>
      <input type="text" name="Telefone" size="40" value={tel} onChange={telChange}/> 
    </div>
    <div>
       <input value="GO" type="button" id= "go">
    </div>
  </div>  
  <script src="script.js"></script>

  <!--
  This script places a badge on your repl's full-browser view back to your repl's cover
  page. Try various colors for the theme: dark, light, red, orange, yellow, lime, green,
  teal, blue, blurple, magenta, pink!
  -->
  <script src="https://replit.com/public/js/replit-badge-v2.js" theme="dark" position="bottom-right"></script>

</html>
  );
}
